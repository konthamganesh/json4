node() {
    stage("checkout") {
        def jsonString = '{"name":"ganesh","age":23}'
        def jsonObj = readJSON text: jsonString

        assert jsonObj['name'] == 'ganesh' 
        sh "echo ${jsonObj.name}"  
        sh "echo ${jsonObj.age}" 
    }
}
